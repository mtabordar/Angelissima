import { Product } from '../../products/shared/product';

export class Registry {
  saleDate: Date;
  quantity: number;
  price: number;
  totalPrice: number;
  productId: number;
  product: Product;
}